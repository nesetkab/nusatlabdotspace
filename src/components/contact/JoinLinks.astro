---
import { joinLinks } from '../site/links';
---

<ul class="space-y-2">
  {joinLinks.map(({ href, label, note }) => (
    <li>
      <a href={href}>{label}</a><span class="text-alice/80">: {note}</span>
    </li>
  ))}
</ul>
