---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={['scroll-mt-20 py-16 sm:py-20', className]}>
  <div class="mx-auto max-w-6xl px-5">
    <slot />
  </div>
</section>
