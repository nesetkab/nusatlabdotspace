---
interface Props {
  title: string;
  sub?: string;
}

const { title, sub } = Astro.props;
---

<h2 class="font-display text-2xl font-medium text-balance text-white sm:text-3xl">{title}</h2>
{sub && <p class="mt-3 max-w-[68ch] text-pretty text-alice/80">{sub}</p>}
