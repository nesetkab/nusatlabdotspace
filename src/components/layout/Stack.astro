---
// Vertical run of <Mission> write-ups, separated by rules.
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['divide-y divide-line [&>*]:py-16 [&>*:first-child]:pt-0 [&>*:last-child]:pb-0', className]}>
  <slot />
</div>
